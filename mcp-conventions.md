# MCP Protocol Conventions

The canonical ecosystem transport specification now lives in
[spore/PROTOCOL.md](/Users/williamnewton/projects/claude-mycelium/spore/PROTOCOL.md).

Use that file for:

- JSON-RPC and MCP framing rules
- request and response envelopes
- error code allocation
- naming conventions
- shared identity fields
- timeout and handshake guidance

This file remains only as a pointer from the root `septa/` directory so
payload schema authors can find the protocol rules without duplicating them.
